// The Blueprint (Class)
class Student {
    // Properties (State)
    int id;
    String name;
    int age;

    // Behavior (Action) Method
    public void displayInfo() {
        System.out.println("ID: " + id + " | Name: " + name + " | Age: " + age);
    }
}
