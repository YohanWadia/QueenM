Future<void> main() async {
  
  List<int> numbers = [1,2,3,4,5];
  List<String> names = ["Yohan", "Aistis", "Igor", "Jonas", "Lukas"];
  
  for (int i = 0; i < 5; i++) {
    print(numbers[i]);
    print(names[i]);
    
  }
}
